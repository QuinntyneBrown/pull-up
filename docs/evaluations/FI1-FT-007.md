# FI1 — FT-007 State components

## What shipped

`projects/components/src/lib/`:

- `empty-state/empty-state.component.{ts,html,scss}` — `pu-empty-state` with inputs `icon: string`, `title: string`, `supportingText: string`. CTA via default `<ng-content>` slot. `role="status"`.
- `error-state/error-state.component.{ts,html,scss}` — `pu-error-state` with inputs `icon`/`title`/`supportingText`/`retryLabel` (all string, sensible defaults) and a `(retry)` output. Built-in `mat-stroked-button` for "Try again". `role="alert"`.
- `loading-skeleton/loading-skeleton.component.{ts,html,scss}` — `pu-loading-skeleton` with `count: number = 3` and `height: string = '88px'`. Renders shimmering Material 3 container rows. Respects `prefers-reduced-motion`.

`public-api.ts` re-exports all three.

## Library placement

All in `projects/components/src/lib/`. Imports only `@angular/material/button`, `@angular/material/icon`. No `api` / `domain` dependencies.

## Acceptance test

Per the plan, FT-007's acceptance is indirectly verified by FT-015 (home page exercises empty/error/loading paths). Components compile and ship in the bundle.

## Build

`ng build pull-up` → 0 warnings, 0 errors.

## L2 coverage

L2-062 (empty state surface), L2-063 (error state with retry) — presentation in place; consumed by FT-015 home page.
