# FI1 — FT-002 ProfileService

## What shipped

`projects/api/src/lib/profile/`:

- `profile.service.contract.ts` — `IProfileService` + `PROFILE_SERVICE` token.
- `profile.service.ts` — concrete implementation.
- 6 DTOs: `UpdateProfileRequest`, `RequestEmailChangeRequest`, `ConfirmEmailChangeRequest`, `DeleteAccountRequest`, `NotificationPreferences`, `UpdateNotificationPreferencesRequest`.
- `profile.service.spec.ts` — 6 TestBed + HttpTestingController cases (one per method).

Methods + URLs:

| Method | Verb | URL | Body |
|---|---|---|---|
| `updateProfile` | PUT | `/api/users/me/profile` | `{ fullName, displayName }` |
| `requestEmailChange` | POST | `/api/users/me/email-change` | `{ newEmail, currentPassword }` |
| `confirmEmailChange` | POST | `/api/users/me/email-change/confirm` | `{ token }` |
| `deleteAccount` | DELETE | `/api/users/me` | `{ currentPassword }` (in body via `HttpClient.delete(..., { body })`) |
| `getNotificationPreferences` | GET | `/api/users/me/notification-preferences` | — |
| `updateNotificationPreferences` | PUT | `/api/users/me/notification-preferences` | `{ newInvitations, eventReminders, rsvpChanges }` |

`public-api.ts` re-exports the token + service + each DTO type.

## Acceptance test

```
Test Files  2 passed (2)
     Tests  12 passed (12)
```

(6 ProfileService + 6 AuthService from FT-001.)

## Build

`ng build pull-up` → 0 warnings, 0 errors.

## Library placement

All artifacts in `projects/api/src/lib/profile/`. No imports from `components` / `domain`. Conforms to FP1 §1.

## L2 coverage

L2-012 (profile edit), L2-013 (email change with confirm), L2-014/L2-015 (delete account with confirm), L2-016 (notification prefs view), L2-017 (notification prefs update) — service layer in place. UI surfaces follow in FT-011..FT-014.
