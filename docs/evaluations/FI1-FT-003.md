# FI1 — FT-003 EventsService

## What shipped

`projects/api/src/lib/events/`:

- `events.service.contract.ts` — `IEventsService` + `EVENTS_SERVICE` token.
- `events.service.ts` — concrete implementation.
- DTOs: `RsvpStatus` (string union `'Going' | 'Maybe' | 'CantGo'`), `EventSummary`, `ListMyEventsResponse` (four-bucket `thisWeek`/`laterThisMonth`/`nextMonth`/`past`), `HostSummary`, `GuestSummary`, `EventDetail`, `CreateEventRequest`, `CreateEventResponse`, `UpdateEventRequest`, `AddInviteeRequest`, `SetRsvpRequest`.
- `events.service.spec.ts` — 9 TestBed + HttpTestingController cases.

Methods + URLs:

| Method | Verb | URL |
|---|---|---|
| `list(scope?)` | GET | `/api/events` (+ optional `?scope=` query) |
| `get(id)` | GET | `/api/events/{id}` |
| `create` | POST | `/api/events` (201 → `{ id }`) |
| `update(id, req)` | PUT | `/api/events/{id}` |
| `cancel(id)` | POST | `/api/events/{id}/cancel` |
| `addInvitee(id, req)` | POST | `/api/events/{id}/invitees` |
| `removeInvitee(id, invId)` | DELETE | `/api/events/{id}/invitees/{invId}` |
| `setRsvp(id, req)` | PUT | `/api/events/{id}/rsvp` |

`public-api.ts` re-exports the token + service + each DTO type.

## Acceptance test

```
Test Files  3 passed (3)
     Tests  21 passed (21)
```

(6 AuthService + 6 ProfileService + 9 EventsService.)

## Build

`ng build pull-up` → 0 warnings, 0 errors.

## Library placement

All artifacts in `projects/api/src/lib/events/`. No imports from `components` / `domain`. Conforms to FP1 §1.

## L2 coverage

L2-018..L2-036 (events CRUD + invitee management + RSVP), L2-039 (event listing scopes) — service layer in place. UI surfaces follow in FT-015..FT-019.
