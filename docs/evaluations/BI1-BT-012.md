# BI1 — BT-012: PUT /api/users/me/profile

## Slice summary

- **Task:** BT-012.
- **L2 IDs:** L2-012 (edit display name + full name).
- **Files added:**
  - `Features/Users/UpdateProfile/{UpdateProfileCommand,Validator,Handler}.cs`
  - `Api/Requests/UpdateProfileRequest.cs`
  - `tests/.../Users/UpdateProfileTests.cs`
- **Files edited:**
  - `Domain/Users/User.cs` — added `UpdateProfile(fullName, displayName)` method (trims + invariant check).
  - `UsersController.cs` — added `[Authorize]` `PUT /api/users/me/profile` returning 204.

## Behavior

Handler reads `ICurrentUserAccessor.UserId`, throws `UnauthorizedAccessException` (→ 401) if missing, loads the user, calls the new domain mutation, saves. Validator enforces `FullName` 1–100 chars and `DisplayName` 1–40 chars. `[AuditedAction("USER_PROFILE_UPDATED")]`.

## Rubric walk

| # | Criterion | Result |
|---|---|---|
| 1 | Guidance | Pass. |
| 2 | Requirements | Pass — happy-path test asserts L2-012 round trip via `/me`. Negative tests cover empty + over-length. Unauthenticated request returns 401. |
| 3 | Radically simple | Pass — 4 small files; no `IProfileService`. |
| 4 | No temp code | Pass. |
| 5 | One type per file | Pass. |
| 6 | SOLID + CQS | Pass. |
| 8 | ATDD | Pass — 4 tests. |
| 10 | Build/run clean | Pass — `dotnet build`: 0/0; `dotnet test`: **37 passed** (4 new + 33 prior). |

## Done

BT-012 complete. Band C 1/4 done.
